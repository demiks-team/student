enum PaymentMethod { _, online, cash, card, processors, check }

enum CourseMaterialPageContentType { _, learningMaterial, file, video }

enum FileType { _, word, excel, powerPoint, pdf, image, video, audio }

enum LevelOfSharingCourseMaterial { _, allTeachers, mySchoolTeachers }

enum ProgramSkill {
  _,
  english,
  french,
  spanish,
  japanese,
  portuguese,
  german,
  russian,
  chinese,
  arabic
}

enum ProgramLevel { _, beginner, intermediate, advanced }

enum GroupSessionStatus { defaultt, cancelled, requested }

enum DeadlineType { _, session, day }
